# C++ 标准演进详解：从 C++98 到 C++26

> 面向汽车电子 / AUTOSAR 开发者的 C++ 语言特性演进手册。
> 每个版本按「语言特性 / 标准库 / 对汽车行业(AUTOSAR)的影响」三条线展开。

---

## 0. 版本时间线速览

| 标准 | ISO 编号 | 发布年 | 定位 | AUTOSAR 相关性 |
|---|---|---|---|---|
| C++98 | ISO/IEC 14882:1998 | 1998 | 第一个国际标准 | MISRA C++:2008 的基线 |
| C++03 | ISO/IEC 14882:2003 | 2003 | 缺陷修订(TC1) | MISRA C++:2008 实际基线 |
| C++TR1 | TR 19768 | 2007 | 库技术报告(非标准) | 部分老工具链仅支持到此 |
| **C++11** | 14882:2011 | 2011 | 现代 C++ 的起点 | Adaptive Platform 早期基线 |
| **C++14** | 14882:2014 | 2014 | C++11 的完善 | **AUTOSAR C++14 指南基线** |
| **C++17** | 14882:2017 | 2017 | 库大幅扩充 | AP R20-11 起编译基线；MISRA C++:2023 基线 |
| **C++20** | 14882:2020 | 2020 | 四大特性(概念/范围/协程/模块) | AP 新版本逐步引入 |
| C++23 | 14882:2024 | 2024(2023定稿) | 打磨版本 | 少量库特性开始被采用 |
| C++26 | 预计 2026 | 进行中 | 反射/契约/std::execution | 未来方向 |

---

## 1. C++98 / C++03 —— 基线

### C++98 确立的核心
- 类、继承、虚函数、多重继承
- **模板**（含类模板、函数模板、模板特化）
- 异常处理 `try/catch/throw`
- 命名空间 `namespace`
- RTTI：`dynamic_cast` / `typeid`
- 四种新式转换：`static_cast` / `const_cast` / `reinterpret_cast` / `dynamic_cast`
- `bool` 类型、`wchar_t`
- 标准库：STL（容器、迭代器、算法）、`std::string`、iostream、`locale`、`std::auto_ptr`

### C++03 的变化
C++03 不是新语言版本，而是 **TC1（Technical Corrigendum 1）**，只修 bug：
- 明确 **值初始化 (value initialization)** 语义
- 明确 `std::vector` 元素**连续存储**保证（这一条对底层内存操作意义重大）
- 修正模板名字查找、`export` 相关缺陷

### 汽车行业影响
- **MISRA C++:2008** 基于 C++03 编写，长期是 Classic Platform 上 C++ 的唯一规范。
- 典型禁令：禁止动态内存、禁止异常、禁止 RTTI、禁止多重继承（非接口）。
- 遗留 MCU 工具链（如老版 GHS、TASKING）常止步于 C++03。

---

## 2. C++11 —— 「感觉像一门新语言」

C++11 是断代式升级，量最大。

### 2.1 类型与初始化
| 特性 | 说明 | 示例 |
|---|---|---|
| `auto` | 类型推导 | `auto it = v.begin();` |
| `decltype` | 表达式类型 | `decltype(a+b) c;` |
| 统一初始化 | 花括号初始化 | `std::vector<int> v{1,2,3};` |
| `std::initializer_list` | 支持自定义类型花括号构造 | |
| `nullptr` | 类型安全空指针，替代 `NULL`/`0` | |
| 强类型枚举 | `enum class Color : uint8_t {Red};` | 不隐式转 int，作用域隔离 |
| `constexpr` | 编译期常量表达式函数/变量 | 只允许单条 return |
| `using` 类型别名 | 支持**模板别名** | `template<class T> using Vec = std::vector<T>;` |
| `long long` | 至少 64 位整型 | |

### 2.2 移动语义（性能层面最重要）
- **右值引用 `T&&`**
- 移动构造 / 移动赋值
- `std::move` / `std::forward`
- **完美转发** + 引用折叠规则
- 返回值优化(RVO)语义明确化

```cpp
class Buffer {
  uint8_t* p_; size_t n_;
public:
  Buffer(Buffer&& o) noexcept : p_(o.p_), n_(o.n_) { o.p_ = nullptr; o.n_ = 0; }
};
```

### 2.3 Lambda 表达式
```cpp
auto f = [capture](int x) mutable -> int { return x + 1; };
```
捕获方式：`[=]` 值捕获、`[&]` 引用捕获、`[this]`、显式列表。

### 2.4 类与函数增强
- `override` / `final`：虚函数意图显式化（**AUTOSAR 强制要求**）
- `= delete` / `= default`
- **委托构造函数**、**继承构造函数** `using Base::Base;`
- **类内成员初始化器** `int x_ = 0;`
- `noexcept` 说明符与运算符
- 尾置返回类型 `auto f() -> int`
- `explicit` 可用于转换运算符

### 2.5 模板增强
- **变参模板 (variadic templates)** `template<class... Ts>`
- 外部模板 `extern template`
- `>>` 不再被误解析为移位
- SFINAE 更明确，`std::enable_if` 惯用法成熟

### 2.6 并发（语言首次引入内存模型）
- **C++ 内存模型**、`std::atomic`、六种 memory_order
- `std::thread` / `std::mutex` / `std::lock_guard` / `std::unique_lock`
- `std::condition_variable`
- `std::future` / `std::promise` / `std::packaged_task` / `std::async`
- `thread_local` 存储期
- 静态局部变量初始化线程安全（"magic static"）

### 2.7 标准库新增
- 智能指针：`unique_ptr` / `shared_ptr` / `weak_ptr`（`auto_ptr` 废弃）
- 容器：`array` / `forward_list` / `unordered_map` / `unordered_set`
- `std::tuple` / `std::function` / `std::bind` / `std::ref`
- `<type_traits>` 类型特征库
- `<chrono>` 时间库
- `<random>` 随机数库
- `<regex>` 正则
- `std::to_string` / `std::stoi` 系列
- 容器 `emplace` 系列、`cbegin/cend`
- 基于范围的 for：`for (auto& e : c)`

### 2.8 其他
- 原始字符串 `R"(...)"`、UTF-8/16/32 字面量
- 用户定义字面量 `operator"" _km`
- `static_assert`
- 属性语法 `[[noreturn]]`
- 对齐控制 `alignas` / `alignof`
- 显式虚析构、`sizeof` 用于非静态成员

### 汽车行业影响
- Adaptive Platform 从设计之初就假定 C++11 以上。
- `enum class`、`override`、智能指针成为 AUTOSAR C++14 指南的推荐/强制项。
- 但 `std::thread`、`std::async`、`shared_ptr` 的动态分配在硬实时/ASIL-D 场景仍受限。

---

## 3. C++14 —— C++11 的完成版（AUTOSAR 基线）

### 语言特性
| 特性 | 说明 |
|---|---|
| **泛型 lambda** | `[](auto x){ return x*2; }` |
| **lambda 初始化捕获** | `[p = std::move(ptr)]{}`（可移动捕获） |
| **返回类型推导** | `auto f() { return 42; }` |
| **变量模板** | `template<class T> constexpr T pi = T(3.1415926);` |
| **放宽 constexpr** | 允许局部变量、循环、多语句、分支 |
| 二进制字面量 | `0b1010'0001` |
| 数字分位符 | `1'000'000` |
| `[[deprecated]]` | 属性 |
| 聚合初始化 | 允许带成员初始化器的聚合体 |
| 变长数组大小的 `new` | `operator new` 对齐重载改进 |

### 标准库
- `std::make_unique`（补上 C++11 的缺口）
- `std::shared_timed_mutex` / `shared_lock`（读写锁）
- `std::integer_sequence` / `index_sequence`
- 透明比较器 `std::less<void>`
- 类型特征别名 `std::enable_if_t` / `decay_t` 等 `_t` 系列
- `std::exchange`、`std::quoted`
- 用户定义字面量 `"s"s`、`1s`（chrono）、`1i`（complex）

### 为什么 AUTOSAR 选 C++14？
**AUTOSAR C++14 Coding Guidelines**（首发 R17-03，后续 R19-03/R19-11/R22-11 更新）选择 C++14 的原因：
1. 2017 年时 C++14 是**编译器支持最成熟且已被认证工具链覆盖**的版本；
2. C++11/14 已提供功能安全所需的表达力（`constexpr`、强类型枚举、RAII）；
3. C++17 当时太新，静态分析工具与 Qualified Compiler 尚未跟上。

该指南共 **342 条规则**，来源包括 MISRA C++:2008、HIC++ v4.0、JSF++、CERT C++。分级为 Required / Advisory / (部分) Automated。

典型规则方向：
- 禁止 `goto`、禁止 C 风格转换、禁止 `union`
- 限制动态内存（仅初始化阶段允许）
- 异常：允许但必须有明确策略（相比 MISRA C++:2008 的全面禁止是放宽）
- 禁止递归、禁止 `<cstdarg>`、禁止 `std::system`
- 要求所有资源 RAII 管理
- 对模板与自动类型推导的可读性约束

> 注：AUTOSAR 已于 2023 年将 C++ 指南工作**移交 MISRA**，形成 **MISRA C++:2023**（基线 C++17）。AUTOSAR C++14 指南进入维护/被替代状态。

---

## 4. C++17 —— 库的大跃进

### 4.1 语言特性
| 特性 | 说明 | 示例 |
|---|---|---|
| **结构化绑定** | 解包 tuple/pair/结构体 | `auto [k, v] = *it;` |
| **if/switch 初始化语句** | 缩小作用域 | `if (auto r = f(); r.ok())` |
| **constexpr if** | 编译期分支，替代大量 SFINAE | `if constexpr (std::is_integral_v<T>)` |
| **内联变量** | 头文件中定义变量 | `inline constexpr int kMax = 10;` |
| **折叠表达式** | 变参包展开 | `return (args + ...);` |
| **CTAD** | 类模板参数推导 | `std::pair p{1, 2.0};` |
| **嵌套命名空间** | `namespace a::b::c {}` | |
| **保证的复制省略** | prvalue 不再需要可拷贝 | |
| `auto` 模板参数 | `template<auto N>` | |
| 强制求值顺序 | 部分表达式求值顺序确定化 | |
| `[[nodiscard]]` `[[maybe_unused]]` `[[fallthrough]]` | 属性 | 安全关键代码强推荐 |
| 十六进制浮点字面量 | `0x1p-3` | |
| `static_assert` 无消息版 | | |
| 异常规格进入类型系统 | `noexcept` 参与重载/函数指针类型 | |

**移除/废弃**：`std::auto_ptr`、`register`、`bool++`、动态异常规格 `throw(X)`、`std::random_shuffle`。

### 4.2 标准库（对 AUTOSAR 极其重要）
| 组件 | 意义 |
|---|---|
| **`std::optional`** | 表达「可能无值」，替代魔数/空指针 |
| **`std::variant`** | 类型安全 union，AUTOSAR 禁 union 的替代方案 |
| **`std::any`** | 类型擦除容器 |
| **`std::string_view`** | 零拷贝字符串视图 |
| **`std::filesystem`** | 文件系统操作（AP 使用） |
| **并行算法** | `std::for_each(std::execution::par, ...)` |
| `std::byte` | 明确的字节类型 |
| `std::apply` / `std::invoke` | 泛型调用 |
| `std::shared_mutex` | 读写锁（无 timed） |
| `std::clamp` / `std::gcd` / `std::lcm` |  |
| `std::to_chars` / `from_chars` | **无分配、无 locale** 的数值转换，实时系统友好 |
| `std::launder`、多态内存资源 `<memory_resource>` | 自定义分配器基础设施 |
| map/set 的 `try_emplace` / `insert_or_assign` / `merge` / `extract` | |
| `std::not_fn`、`std::void_t`、`std::conjunction` | |

### 汽车行业影响
- **`ara::core` 直接映射 C++17 概念**：`ara::core::Optional`≈`std::optional`、`ara::core::StringView`、`ara::core::Result`（受 Rust Result 与 `std::expected` 提案启发）、`ara::core::ErrorCode`——用返回值错误处理替代异常。
- AP R20-11 及之后规范文本以 C++17 为语言基线。
- **MISRA C++:2023** 以 C++17 为基线，180 余条规则，是 AUTOSAR C++14 的官方后继。

---

## 5. C++20 —— 第二次断代升级

### 5.1 四大特性

**① Concepts（概念）**
```cpp
template<typename T>
concept Arithmetic = std::is_arithmetic_v<T>;

template<Arithmetic T> T add(T a, T b) { return a + b; }
```
好处：模板错误信息可读、约束显式化——对安全关键代码的**可审查性**是巨大提升。

**② Ranges（范围库）**
```cpp
auto evens = data | std::views::filter([](int i){return i%2==0;})
                  | std::views::transform([](int i){return i*i;});
```
惰性求值、无中间容器（**无额外堆分配**，适合嵌入式）。

**③ Coroutines（协程）**
`co_await` / `co_yield` / `co_return`。语言只提供机制，库支持要等 C++23 的 `std::generator`。AP 的异步方法调用可用其简化。

**④ Modules（模块）**
```cpp
export module math;
export int add(int, int);
```
替代头文件：编译加速、无宏泄漏、显式接口。但 2026 年工具链支持仍不完整，构建系统（CMake/Bazel）集成尚在成熟中。

### 5.2 其他语言特性
| 特性 | 说明 |
|---|---|
| **三向比较 `<=>`** | 「宇宙飞船运算符」，`= default` 自动生成全部比较 |
| **指定初始化器** | `Point p{.x = 1, .y = 2}`（来自 C） |
| `consteval` | 必须编译期求值的函数 |
| `constinit` | 保证静态初始化，避免 static init order 问题 |
| constexpr 大幅放宽 | 支持 `try/catch`、虚函数、`dynamic_cast`、union、**动态分配(编译期)** |
| **`[[likely]]` / `[[unlikely]]`** | 分支预测提示 |
| `[[no_unique_address]]` | 空成员优化 |
| **模板 lambda** | `[]<typename T>(T x){}` |
| 缩写函数模板 | `void f(auto x)` |
| 聚合体的 CTAD / 带括号聚合初始化 | |
| `char8_t` | UTF-8 独立类型 |
| 条件 `explicit(bool)` | |
| 位域默认初始化、嵌套 inline 命名空间 | |
| `using enum` | 引入枚举名到作用域 |
| 结构化绑定支持 static/thread_local、可捕获 | |
| 允许 lambda 在无捕获时默认构造/赋值 | |

### 5.3 标准库
- **`std::span`**：非拥有连续内存视图（**替代裸指针+长度**，安全关键代码强力推荐）
- `<bit>`：`std::bit_cast`、`popcount`、`countl_zero`、`has_single_bit`、`rotl/rotr`、`endian`
- `<format>`：`std::format("{:.2f}", x)`，类型安全、无 iostream 开销
- `<numbers>`：`std::numbers::pi`
- `std::atomic_ref`、`std::atomic<shared_ptr>`、原子的 `wait/notify`
- **`std::jthread`**（自动 join）+ **`std::stop_token`**（协作取消）
- `std::latch` / `std::barrier` / `std::counting_semaphore`
- `std::chrono` 日历与时区（`year_month_day`、`zoned_time`）
- `std::source_location`（替代 `__FILE__`/`__LINE__` 宏）
- `constexpr` 化的 `std::vector` / `std::string`
- `std::erase` / `erase_if` 统一擦除
- `std::to_array`、`std::ssize`、`std::midpoint`、`std::lerp`
- `std::is_constant_evaluated()`
- `contains()` 成员（关联容器）

### 汽车行业影响
- `std::span` 与 Concepts 是安全关键代码最受欢迎的两项。
- Coroutines/Modules 在 ASIL 认证工具链上落地慢：需要 Qualified Compiler 与静态分析器双重支持。
- AP 的 `ara::core::Span` 直接对应 `std::span`。

---

## 6. C++23 —— 打磨与补齐

### 语言特性
| 特性 | 说明 |
|---|---|
| **`if consteval`** | 区分编译期/运行期路径 |
| **显式对象参数（Deducing this）** | `auto f(this Self&& self)`，消除 CRTP 与 const/非 const 重复 |
| **`static operator()`** | 无状态 lambda/函子零开销 |
| **多维下标 `operator[](a,b)`** | 配合 `std::mdspan` |
| `auto(x)` / `auto{x}` | 显式衰减拷贝 |
| `#elifdef` / `#elifndef` / `#warning` | 预处理器 |
| `[[assume(expr)]]` | 优化假设 |
| UTF-8 源文件正式化 | |
| 放宽 constexpr（允许非 constexpr 的静态/thread_local 声明存在） | |
| lambda 属性位置放宽、`static_assert` 消息可用户定义(部分) | |
| 移除垃圾回收支持接口 | |

### 标准库（亮点很多）
| 组件 | 说明 |
|---|---|
| **`std::expected<T, E>`** | 值或错误，标准化了 `ara::core::Result` 的模式 |
| **`std::mdspan`** | 多维数组视图，矩阵/图像处理 |
| **`std::flat_map` / `flat_set`** | 基于连续存储的关联容器，**cache 友好、可预测**——嵌入式非常受欢迎 |
| **`std::generator`** | 协程生成器，让 C++20 协程真正可用 |
| **`std::print` / `std::println`** | `std::print("{}\n", x)`，替代 printf/cout |
| `std::stacktrace` | 栈回溯 |
| `std::byteswap` | |
| `std::to_underlying` | 枚举转底层类型 |
| `std::unreachable()` | |
| `std::move_only_function` | 只可移动的 `std::function` |
| `std::out_ptr` / `inout_ptr` | C API 互操作 |
| `std::spanstream` | 固定缓冲区流 |
| Ranges 扩容 | `zip` / `adjacent` / `chunk` / `slide` / `join_with` / `stride` / `cartesian_product` / `enumerate`、`ranges::to` |
| `std::optional` 的 monadic 操作 | `and_then` / `transform` / `or_else` |
| `std::string::contains`、`resize_and_overwrite` | |
| `constexpr` 化 `<cmath>` 部分、`std::unique_ptr` | |
| `import std;` 标准库模块 | |

---

## 7. C++26 —— 进行中（预计 2026 发布）

> 以下为已合入工作草案或高置信度特性，最终以 ISO 定稿为准。

### 语言方向
- **静态反射 (Reflection, P2996)**：`^^T`、`std::meta::members_of`，编译期代码生成——有望取代大量宏与代码生成器（对 AUTOSAR RTE / ARXML 代码生成意义重大）
- **契约 (Contracts, P2900)**：`pre(...)`、`post(...)`、`contract_assert(...)`——直接服务于功能安全的前置/后置条件表达
- **`std::execution`（Senders/Receivers, P2300)**：结构化并发框架
- **Hazard pointers / RCU**：无锁数据结构基础设施
- **`std::simd`**：数据并行类型
- **Pack indexing** `pack...[N]`、结构化绑定作为包
- **删除函数带原因** `= delete("reason")`
- **未初始化读为错误化（Erroneous behaviour）**：把一类 UB 降级为「错误行为」，可诊断——安全关键代码重大改进
- `#embed`（来自 C23，嵌入二进制资源）
- 移除 C++ 中的 `std::rand` 相关不安全接口、废弃部分旧库

### 库方向
- `std::text_encoding`、`std::inplace_vector`（固定容量、无堆分配 vector —— 嵌入式期待已久）
- `std::function_ref`、`std::submdspan`
- 调试支持 `std::breakpoint` / `is_debugger_present`
- 更多 constexpr 化（容器、`<cmath>`）

---

## 8. AUTOSAR 视角的横向对照

### 8.1 编码规范演进链
```
MISRA C++:2008 (C++03)
        │
        ▼
AUTOSAR C++14 Guidelines (C++14, 342 条, R17-03 → R22-11)
        │  2023 年移交 MISRA
        ▼
MISRA C++:2023 (C++17, ~180 条)  ← 当前推荐
        │
        ▼
(未来) 覆盖 C++20/23 的版本
```

### 8.2 `ara::core` 与标准库映射
| ara::core | 标准库对应 | 引入标准 |
|---|---|---|
| `ara::core::Optional` | `std::optional` | C++17 |
| `ara::core::StringView` | `std::string_view` | C++17 |
| `ara::core::Span` | `std::span` | C++20 |
| `ara::core::Variant` | `std::variant` | C++17 |
| `ara::core::Result` / `ErrorCode` | `std::expected` | C++23 |
| `ara::core::Vector` / `Map` / `String` | 带自定义 Allocator 的 std 容器 | C++11 |
| `ara::core::Array` | `std::array` | C++11 |
| `ara::core::Future` / `Promise` | `std::future` / `promise`（扩展了 `then`） | C++11 |
| `ara::core::SteadyClock` | `std::chrono::steady_clock` | C++11 |

> 设计意图：`ara::core` 在标准库尚未提供或 ABI/异常策略不合适时提供受控替代，并保证跨 AP 实现的可移植性。

### 8.3 安全关键场景下的特性取舍
| 特性 | Classic Platform | Adaptive Platform | 备注 |
|---|---|---|---|
| 异常 | 通常禁用 (`-fno-exceptions`) | 允许但受限 | AP 倾向 `Result` 返回值 |
| RTTI / `dynamic_cast` | 禁用 | 谨慎使用 | 代码量与不确定性 |
| 动态内存 | 仅初始化阶段 | 允许，需自定义 Allocator | 实时性与碎片 |
| 模板 | 受限（代码膨胀） | 广泛使用 | 需关注 flash 占用 |
| 虚函数 | 允许，需 `override`/`final` | 允许 | vtable 位于只读段 |
| STL 容器 | 少用，多用固定容量容器 | `ara::core` 容器 | 期待 `std::inplace_vector` |
| 协程 | 不用 | 试验阶段 | 隐式堆分配是障碍 |
| 模块 | 不用 | 试验阶段 | 认证工具链未跟上 |

### 8.4 各版本对功能安全最有价值的特性 TOP 清单
- **C++11**：`enum class`、`override`/`final`、`constexpr`、`static_assert`、`nullptr`、RAII 智能指针
- **C++14**：放宽的 `constexpr`、`make_unique`、`[[deprecated]]`
- **C++17**：`std::optional`、`std::variant`、`[[nodiscard]]`、`constexpr if`、`to_chars`
- **C++20**：`std::span`、Concepts、`constinit`、`[[likely]]`、`std::bit_cast`、`source_location`
- **C++23**：`std::expected`、`flat_map`、`to_underlying`、`unreachable`
- **C++26**：Contracts、Reflection、Erroneous behaviour、`inplace_vector`

---

## 9. 编译器与工具链现实（截至 2026 年中）

| 编译器 | C++17 | C++20 | C++23 | 汽车认证情况 |
|---|---|---|---|---|
| GCC | 完整 (7+) | 基本完整 (13+，Modules 部分) | 大部分 (14/15) | 需商业 Qualified 版本（如 Bosch/Validas 认证包） |
| Clang | 完整 (5+) | 基本完整 (17+) | 进行中 | 部分厂商提供认证版 |
| MSVC | 完整 | 完整含 Modules | 进行中 | 汽车少用 |
| GHS MULTI | 支持 | 部分 | 少 | ISO 26262 ASIL-D 认证 |
| TASKING / IAR | C++14/17 | 少 | 无 | 认证工具链 |

**实践建议**
1. 语言标准选择受**认证编译器**限制，而非最新标准；量产项目常年落后标准 1~2 代。
2. 静态分析工具（Coverity / Polyspace / Axivion / Helix QAC / PC-lint）对新标准支持是关键卡点。
3. 新特性引入前需评估：编译器 bug 记录、生成代码可预测性、是否引入隐式动态分配、是否可被静态分析覆盖。
4. Classic Platform（MCU，ASIL-D）建议保守：C++14 子集 + MISRA；Adaptive Platform（SoC，Linux/QNX）可用 C++17，逐步引入 C++20 的 `span`/Concepts。

---

## 10. 一页速查：升级时最该先用的特性

```cpp
// C++11：类型安全枚举 + 显式虚函数
enum class State : std::uint8_t { kIdle, kRun };
void Update() override;

// C++14：编译期计算
constexpr std::uint32_t Crc32(const std::uint8_t* d, std::size_t n) { /* 循环 OK */ }

// C++17：无魔数错误表达
std::optional<Config> LoadConfig();
if (auto c = LoadConfig(); c.has_value()) { /* ... */ }

// C++20：安全的缓冲区传递（替代 (ptr, len)）
void Parse(std::span<const std::uint8_t> frame);

// C++23：显式的错误传播
std::expected<Value, ErrorCode> Read(Address a);
```

---

## 参考
- ISO/IEC 14882 各版本标准文本
- cppreference.com «C++ compiler support»
- AUTOSAR «Guidelines for the use of the C++14 language in critical and safety-related systems» (R22-11)
- AUTOSAR «Specification of Adaptive Platform Core» (`ara::core`)
- MISRA C++:2023
- WG21 论文集（P2996 Reflection、P2900 Contracts、P2300 std::execution）
